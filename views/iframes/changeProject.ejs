<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/layui_dist/css/layui.css">
</head>
<body>
  <form class="layui-form" action="">
    <div class="layui-form-item" >
      <div class="layui-inline">
        <label class="layui-form-label">项目名称</label>
        <div class="layui-input-inline">
          <input type="text" name="nameOfProject" id="nameOfProject" placeholder="请输入项目名称" lay-verify="required|text" lay-reqtext="请填写项目名称" autocomplete="off" class="layui-input" style="width: 200px">
        </div>
      </div>
    </div>
    <div class="layui-form-item">
      <div class="layui-inline">
        <label class="layui-form-label">开始日期</label>
        <div class="layui-input-inline">
          <input type="text" name="startdateOfProject" id="startdate" placeholder="yyyy-MM-dd" autocomplete="off" class="layui-input">
        </div>
      </div>
      <div class="layui-inline">
        <label class="layui-form-label">结束日期</label>
        <div class="layui-input-inline">
          <input type="text" name="enddateOfProject" lay-verify="compareDate" id="enddate" placeholder="yyyy-MM-dd" autocomplete="off" class="layui-input">
        </div>
      </div> 
    </div>
    <div class="layui-form-item">
      <div class="layui-inline layui-bg-red">
        修改起止日期会使得部分人力安排被删除，请确认后再进行修改操作。
      </div>
    </div>
    <div class="layui-form-item">
      <div class="layui-input-block">
        <button class="layui-btn" lay-submit="submit" lay-filter="changeform">提交</button>
        <button type="reset" class="layui-btn layui-btn-primary">重置</button>
      </div>
    </div>
  </form>
<script src="/layui_dist/layui.js"></script>
<script src="/moment/moment.js"></script>
<script>
layui.use(['form','laydate'],function(){
  var form = layui.form;
  var laydate = layui.laydate;
  $ = layui.$;
  var openedProject = layui.data('openedProject');
  $("#nameOfProject").val(openedProject.nameOfProject);
  $('#startdate').val(openedProject.startdateOfProject);
  $('#enddate').val(openedProject.enddateOfProject);
  form.verify({
    compareDate: function (value,item) {
      var startdate = $('#startdate').val();
      if (moment(value)-moment(startdate)<0) {
        return '结束日期必须大于开始日期';
      }
    }
  });
  form.on('submit(changeform)',function(data){
    data.field['_id'] = openedProject._id;
    $.ajax({
      type: "get",
      url: "/project/changeProjectValue",
      data: data.field,
      dataType: "json",
      success: function (res) {
        layui.data('res',{key:"SUCCESS",value:res.SUCCESS});
        var thisIframe = parent.layer.getFrameIndex(window.name);
        parent.layui.layer.close(thisIframe);
      }
    });
  });
  laydate.render({
    elem:"#startdate"
  });
  laydate.render({
    elem:"#enddate"
  })
})
</script>
</body>
</html>